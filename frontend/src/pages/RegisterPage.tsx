import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { api } from "../api/axios";
import { Button, Input } from "../components/ui";
import { AuthShell, Field } from "./LoginPage";
export function RegisterPage() { const navigate = useNavigate(); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); const submit = async (e: React.FormEvent<HTMLFormElement>) => { e.preventDefault(); const form = new FormData(e.currentTarget); setBusy(true); setError(""); try { await api.post("/auth/register", { username: form.get("username"), email: form.get("email"), password: form.get("password") }); navigate("/login", { state: { registered: true } }); } catch (err: any) { setError(err.response?.data?.message ?? "ไม่สามารถสมัครสมาชิกได้"); } finally { setBusy(false); } }; return <AuthShell title="สร้างบัญชีใหม่" subtitle="กระเป๋า Custodial นี้สำหรับการศึกษาและ Sepolia Testnet เท่านั้น"><form onSubmit={submit} className="space-y-4"><Field label="ชื่อผู้ใช้"><Input name="username" required minLength={3} maxLength={30} placeholder="white" /></Field><Field label="อีเมล"><Input name="email" type="email" required placeholder="you@example.com" /></Field><Field label="รหัสผ่าน"><Input name="password" type="password" required minLength={8} maxLength={72} placeholder="อย่างน้อย 8 ตัวอักษร" /></Field>{error && <p className="text-sm text-red-300">{error}</p>}<Button disabled={busy} className="w-full">{busy ? "กำลังสร้างบัญชี..." : "สมัครสมาชิก"}</Button><p className="text-center text-sm text-white/45">มีบัญชีแล้ว? <Link className="text-violet-300 hover:text-violet-200" to="/login">เข้าสู่ระบบ</Link></p></form></AuthShell>; }
